import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
  },
  helloWorld: {
    color: '#000000',
    fontSize: 16,
    alignSelf: 'center',
  },
});

export default styles;
