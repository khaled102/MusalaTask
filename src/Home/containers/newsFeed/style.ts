import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  helloWorld: {
    color: '#000000',
    fontSize: 16,
    alignSelf: 'center',
  },
});

export default styles;
